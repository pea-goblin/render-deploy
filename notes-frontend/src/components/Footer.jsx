const Footer = () => {
    const footerStyle = {
        color: "green",
        fontStyle: "italic"
    }

    return (
        <div style={footerStyle}>
            Note app, FSO, 2025
        </div>
    );
}

export default Footer;